# Handoff — 2026-09-13 14:04

## Goal

`mgr-servers` 저장소의 `.claude` 아래에서만 쓰던 worktree 작업 흐름(`/wt` · `/wt-epic` · `wt-done`)을 **Claude Code 플러그인으로 떼어내어**, 다른 컴퓨터·다른 프로젝트에서 복사 붙여넣기 없이 설치해 쓴다.

설치 경로는 다음과 같다.

```
/plugin marketplace add junghosuh/claude-plugins
/plugin install herdr-wt@claude-plugins
```

## Current Progress

### 확정한 결정 4건

| 항목 | 결정 | 근거 |
|---|---|---|
| 저장소(마켓플레이스) 이름 | `claude-plugins` | GitHub 계정(`junghosuh`)으로 이미 구분되므로 이름에 개인 표시를 중복하지 않는다. 앞으로 다른 플러그인을 같은 저장소에 얹으면 `marketplace add` 를 컴퓨터마다 한 번만 실행한다 |
| 첫 플러그인 이름 | `herdr-wt` | herdr 결합을 이름에 명시해 herdr 없는 환경용 폴백 설계 부담을 없앤다. 스킬 호출은 `herdr-wt:wt-done` |
| herdr 의존 | 전용으로 고정 | `HERDR_ENV` 검사 실패 시 중단하는 현행 동작을 유지한다. `EnterWorktree` 폴백은 필요해지면 그때 더한다 |
| Jira 연동 | 저장소별 설정 파일로 외부화 | 플러그인은 절차만 갖고, 프로젝트 키·전환 id·베이스 브랜치는 `.claude/wt.json` 에서 읽는다. 설정이 없으면 Jira 단계를 생략하고 worktree 만 처리한다 |

### 생성한 구조

`/Users/junghosuh/Projects/jh/claude-plugins` — `git init` 까지 실행했고 **커밋은 하지 않았다.**

```
claude-plugins/
  .claude-plugin/marketplace.json          # name: claude-plugins, plugins: [herdr-wt]
  plugins/herdr-wt/
    .claude-plugin/plugin.json             # v0.1.0
    commands/wt.md                         # 원본 그대로 복사
    commands/wt-epic.md                    # 원본 그대로 복사
    skills/wt-done/SKILL.md                # 원본 그대로 복사
```

세 문서는 **원본을 그대로 복사한 상태다.** 아래 「Next Steps」의 개조를 아직 하지 않았다.

### 원본 위치

`/Users/junghosuh/Projects/MasterGirl/mgr-servers/.claude`

| 파일 | 역할 |
|---|---|
| `commands/wt.md` | 이슈키 → 브랜치명 작명 → `herdr worktree create` → 그 pane 에 에이전트 기동 → Jira 를 진행 중으로. 작업 자체는 하지 않는다 |
| `commands/wt-epic.md` | 에픽 하위 일감을 의존성 순으로 팬아웃하고 머지 순서를 통제한다 |
| `skills/wt-done/SKILL.md` | 0 사전점검+승인 → 1 PR squash 머지 → 2 Jira 완료+결과 코멘트 → 3 pane 정리 → 4 worktree 제거(세션 동반 종료). 순서가 고정이다 |
| `rules/worktree.md` | 공통 규칙 — develop 은 worktree 에서 체크아웃 불가, `mise trust`, `EnterWorktree` 격리 제약 |

**원본은 계속 `mgr-servers` 에 남아 있다.** 플러그인으로 대체할지, 양쪽을 함께 둘지는 아직 정하지 않았다.

## What Worked

- **설치된 마켓플레이스 실물로 구조를 확인했다.** 추측하지 않았다 — `~/.claude/plugins/marketplaces/` 아래 `claude-plugins-official` · `ponytail` · `obsidian-skills` 를 읽어 두 형태를 구분했다.
  - 여러 플러그인: `"source": "./plugins/<이름>"` 로 저장소 안 상대경로를 가리킨다 (공식 마켓플레이스)
  - 단일 플러그인: `"source": "./"` 로 저장소 루트 자체가 플러그인이다 (`ponytail` · `obsidian-skills`)
- **`plugins/` 구조를 먼저 잡은 판단.** 단일 형태로 시작하면 둘째 플러그인을 넣을 자리가 없어 파일을 전부 옮기고 `source` 를 고쳐야 한다. 지금 잡아 두면 추가 비용이 디렉터리 하나와 JSON 4줄이다.
- `marketplace.json` 에 `renames` 필드가 있다 — **플러그인 이름은 나중에 바꿔도 마이그레이션 경로가 있다.** 신중할 대상은 저장소(마켓플레이스) 이름 쪽이다.

## What Didn't Work

- **`clangd-lsp` 는 `plugin.json` 이 없다.** 공식 마켓플레이스의 플러그인 셋을 훑을 때 확인했다 — `marketplace.json` 항목만으로도 성립하는 경우가 있으니 "모든 플러그인에 `plugin.json` 이 있다"고 가정하지 않는다.

## Next Steps

우선순위 순이다.

### 1. `rules/worktree.md` 의 이주처를 정한다 — 가장 큰 건

`wt.md` 와 `wt-done/SKILL.md` 가 본문에서 `.claude/rules/worktree.md` 경로를 **직접 가리킨다.** 플러그인은 rules 를 배포하지 못하므로(플러그인이 제공하는 것은 commands · skills · agents · hooks · MCP 다) 그 경로가 설치처에 존재하지 않는다.

거기 담긴 것은 셋이다 — develop 은 어떤 worktree 에서도 체크아웃되지 않는다 · worktree 생성 후 `mise trust` · `EnterWorktree` 격리 제약.

선택지는 둘이다.

- `skills/wt-done/references/worktree.md` 로 넣고 문서의 참조 경로를 그쪽으로 고친다
- 필요한 문단만 `wt.md` · `SKILL.md` 본문에 흡수하고 파일을 없앤다

`mise` 는 `mgr-servers` 고유 도구라 범용 플러그인에 남길지도 함께 판단한다.

### 2. `.claude/wt.json` 설정 스키마를 정하고 문서를 그 값으로 바꾼다

현재 문서에 박혀 있는 값들이다.

| 박힌 값 | 위치 |
|---|---|
| Jira 프로젝트 `MGR` | `wt.md` 2단계, `wt-epic.md` 2절 |
| 전환 id — 진행 중 `31` · 완료 `41` · 개발하기로 선택됨 `21` · 백로그 `11`, 상태 id `10092` 등 | `wt.md` 8단계, `SKILL.md` 2단계 표 |
| 담당자 accountId `5fbf0cf59592df0076ea8350` (클라 인수인계 대상) | `SKILL.md` 2단계 |
| `--base develop` | `wt.md` 4단계, `rules/worktree.md` |
| 브랜치 종류 `feature` · `fix` · `docs` · `chore` | `wt.md` 3단계 |
| squash 전용 · `deleteBranchOnMerge` 켜짐 | `SKILL.md` 1단계 |
| proto 경로 `game-server/build/protobuf/src/` | `SKILL.md` 0단계 클라 후속 신호 ① |
| CI·리뷰가 항상 비어 조회하지 않는다는 판정 | `SKILL.md` 0단계 |

초안은 다음과 같다.

```json
{
  "base": "develop",
  "branchKinds": ["feature", "fix", "docs", "chore"],
  "merge": { "squashOnly": true, "deleteBranchOnMerge": true },
  "jira": {
    "project": "MGR",
    "transitions": { "inProgress": "31", "done": "41" },
    "clientHandoffAssignee": "5fbf0cf59592df0076ea8350"
  },
  "protoPaths": ["game-server/build/protobuf/src/"]
}
```

**설정이 없을 때의 동작을 문서에 명시한다** — Jira 절을 통째로 건너뛰고 worktree 와 에이전트만 준비한다.

### 3. 저장소 판례를 남길지 뺄지 정한다

`MGR-85`(유형은 「작업」이었으나 결함 수정이라 `fix/` 로 갔다) · `MGR-182`(proto 4파일을 바꾸고도 클라 이슈 없이 닫혔다) · `MGR-191`(클라 이슈 요약 관례) 등이다. 판단 근거로서 가치는 있으나 다른 프로젝트에서는 노이즈다. 규칙만 남기고 이슈키는 빼는 절충도 가능하다.

### 4. README 와 첫 커밋

설치 방법 · `wt.json` 스키마 · herdr 전제를 적는다. 그 뒤 GitHub 에 `junghosuh/claude-plugins` 로 올리고 다른 컴퓨터에서 `marketplace add` 로 실측한다.

### 5. `mgr-servers` 원본 처리

플러그인이 동작을 확인받은 뒤에 정한다 — 원본을 지우고 플러그인으로 대체할지, 양쪽을 함께 둘지. `wt-epic.md` 가 `.claude/commands/wt.md` 와 `.claude/skills/wt-done/SKILL.md` 를 경로로 참조하므로, 지울 때 그 참조도 함께 고친다.

## 참고

- 이 저장소는 `git init` 만 되어 있고 커밋이 없다. 첫 커밋 전이라 파일을 자유롭게 옮겨도 이력이 지저분해지지 않는다.
- 작업 디렉터리가 `/Users/junghosuh/Projects/jh` 에서 `/Users/junghosuh/Projects/jh/claude-plugins` 로 옮겨졌다.
